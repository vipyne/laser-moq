// Copy to config.js (gitignored) for local testing. In production the Pages
// workflow writes config.js from the repo Actions variable MOQ_RELAY_URL.
window.MOQ_RELAY_URL = "https://your-relay.example.com/anon";
